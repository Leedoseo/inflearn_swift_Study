import UIKit

// 문자와 문자열

// "" 안에 문자가 들어가면 문자열
"Have a nice day" // 문자열
"123" // 문자열
"1" // 문자열

let oneChar: Character = "1" // 문자

// let doubleChar: Character = "22" // 문자가 두개가 들어갔기 때문에 문자열임. : Charact로 지정해서 오류 발생!

// let emptyCharacter: Character = "" // 이건 오류 발생
let emptyCharacter: Character = " " // 이건 오류 발생 X -> 공백이 하나의 문자로 판단
